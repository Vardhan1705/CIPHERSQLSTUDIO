# CIPHERSQLSTUDIO
CipherSQLStudio provides:  A React-based UI to attempt SQL assignments and run SQL queries in a safe sandbox. A Node.js mock backend (server.js) that validates queries and returns simulated results. Optional Docker Compose that can spin up PostgreSQL and Mongo (for when you want a full DB-backed environment).
